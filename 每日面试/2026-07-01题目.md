# 2026-07-01 全栈偏前端面试题

## 1. `MutationObserver`、Promise、`requestAnimationFrame` 的执行顺序

- 题型：八股文 / 输出分析
- 难度：中高级
- 来源或考察方向：综合整理；浏览器事件循环与渲染时机
- 题目正文：一次点击回调中同时修改 DOM、注册 `MutationObserver`、创建已决议 Promise，并调用 `requestAnimationFrame`。说明 Promise 回调、MutationObserver 回调、下一帧 `requestAnimationFrame` 的大致执行顺序；如果微任务持续追加，会对渲染造成什么影响？

## 2. TypeScript `const` 类型参数解决了什么问题

- 题型：八股文 / 类型设计
- 难度：中高级
- 来源或考察方向：[TypeScript 5.0 Release Notes](https://www.typescriptlang.org/docs/handbook/release-notes/typescript-5-0.html#const-type-parameters)
- 题目正文：比较 `function define<T>(value: T)` 与 `function define<const T>(value: T)` 对对象字面量、只读元组的推断结果。它和调用处写 `as const` 有何区别？设计配置 API 时何时应该使用 `const` 类型参数？

## 3. ES Iterator Helpers 与惰性求值

- 题型：八股文 / 场景题
- 难度：中级
- 来源或考察方向：[MDN Iterator helpers](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Iterator#iterator_helpers)
- 题目正文：解释 Iterator Helpers 中 `map`、`filter`、`take` 的惰性执行特征。与数组链式调用相比，它在大数据或无限序列中有什么优势？哪些操作会真正消费迭代器，迭代器被消费后有哪些陷阱？

## 4. 用 `AbortSignal` 统一取消多个异步操作

- 题型：手写题 / 场景题
- 难度：中高级
- 来源或考察方向：[MDN AbortSignal](https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal)
- 题目正文：实现 `loadPageData(signal)`，并行请求用户信息和推荐列表；任一请求失败时取消另一请求，外部路由切换也能取消全部请求。说明如何组合外部 signal、超时和内部失败取消，并区分“主动取消”与真实业务错误。

## 5. 第三方 Cookie 限制与存储分区

- 题型：八股文 / 场景设计
- 难度：高级
- 来源或考察方向：[MDN Storage Partitioning](https://developer.mozilla.org/en-US/docs/Web/Privacy/Guides/State_Partitioning)
- 题目正文：一个嵌入多个客户站点的 iframe 控制台依赖 Cookie、localStorage 和 BroadcastChannel。浏览器启用第三方 Cookie 限制与状态分区后，登录和跨站同步可能出现哪些变化？请设计兼容方案，并说明 Storage Access API、顶层跳转和 token 通信的安全边界。

## 6. React `use` 与 Suspense 的数据读取边界

- 题型：八股文 / 场景题
- 难度：高级
- 来源或考察方向：[React `use` API](https://react.dev/reference/react/use)
- 题目正文：React 中 `use(promise)` 与在 Effect 中请求数据有何不同？为什么在客户端组件渲染时临时创建 Promise 容易反复挂起？请说明 Promise 稳定性、Suspense 边界、错误边界和服务端向客户端传递 Promise 时的设计原则。

## 7. Vue 3.4+ `defineModel` 的双向绑定契约

- 题型：八股文 / 组件设计
- 难度：中高级
- 来源或考察方向：[Vue Component `v-model`](https://vuejs.org/guide/components/v-model.html)
- 题目正文：`defineModel()` 如何对应 `modelValue` 与 `update:modelValue`？如何支持多个 `v-model`、修饰符及 get/set 转换？为什么子组件默认值与父组件未传值时可能产生状态不同步，组件库应如何规避？

## 8. 手写支持通配符订阅的 EventEmitter

- 题型：手写实现
- 难度：中高级
- 来源或考察方向：综合整理；事件系统、数据结构与资源清理
- 题目正文：实现一个 `EventEmitter`，支持 `on(event, handler)`、`once`、`off`、`emit`，并支持 `on('*', handler)` 监听全部事件。要求处理回调执行期间取消订阅、同一函数重复订阅、异常隔离，并返回便于组件卸载时调用的取消函数。

## 9. 算法：最小覆盖子串

- 题型：算法题
- 难度：中高级
- 来源或考察方向：[LeetCode 76. Minimum Window Substring](https://leetcode.com/problems/minimum-window-substring/)
- 题目正文：给定字符串 `s` 和 `t`，返回 `s` 中涵盖 `t` 所有字符的最短连续子串；字符出现次数必须满足要求，不存在则返回空串。要求给出滑动窗口解法、正确处理重复字符，并分析时间与空间复杂度。

## 10. Next.js Partial Prerendering 的静态壳与动态岛边界

- 题型：场景设计
- 难度：高级
- 来源或考察方向：[Next.js Partial Prerendering](https://nextjs.org/docs/app/getting-started/partial-prerendering)
- 题目正文：商品详情页包含可缓存商品信息、依赖 Cookie 的会员价和实时库存。如何划分静态壳、动态内容与 Suspense fallback？哪些动态 API 会使区域转为动态渲染？如何避免把用户数据写入共享缓存，并验证首屏与流式渲染效果？

## 11. Node.js Permission Model 的能力与局限

- 题型：八股文 / 安全场景
- 难度：高级
- 来源或考察方向：[Node.js Permission Model](https://nodejs.org/api/permissions.html)
- 题目正文：一个执行第三方转换插件的 Node.js 服务需要读取指定上传目录，但不应访问环境外文件、派生进程或任意网络。如何使用 Permission Model 缩小权限？它为什么不能替代容器、系统用户隔离或沙箱？依赖在启动阶段和运行阶段分别可能遇到什么问题？

## 12. NestJS 输入验证与输出序列化的安全边界

- 题型：场景题 / 项目经验
- 难度：中高级
- 来源或考察方向：[NestJS Validation](https://docs.nestjs.com/techniques/validation)、[NestJS Serialization](https://docs.nestjs.com/techniques/serialization)
- 题目正文：设计用户更新接口时，如何配置 DTO、`ValidationPipe` 和序列化，防止 mass assignment、未知字段透传、密码哈希泄露与类型隐式转换误判？说明 `whitelist`、`forbidNonWhitelisted`、`transform` 和响应 DTO 各自负责什么。

## 13. 乐观锁解决库存并发更新

- 题型：系统设计 / 数据库
- 难度：中高级
- 来源或考察方向：综合整理；并发控制、接口语义与重试
- 题目正文：多个请求同时扣减同一商品库存，如何用 `version` 字段实现乐观锁？给出核心 SQL 或 ORM 伪代码，说明如何判断冲突、何时重试、如何避免超卖，以及它与悲观锁、数据库原子条件更新的取舍。

## 14. AI 前端的生成式 UI 与可验证状态

- 题型：场景设计 / AI 前端
- 难度：高级
- 来源或考察方向：综合整理；生成式 UI、流式工具调用与状态一致性
- 题目正文：Agent 可流式生成图表、表单和“确认付款”卡片。前端应如何把模型文本、工具调用状态和业务组件状态分层？如何处理参数增量、组件白名单、Schema 校验、刷新恢复、重复 tool call，以及高风险操作的二次确认？

## 15. MCP Elicitation 的交互与安全设计

- 题型：八股文 / Agent 场景题
- 难度：高级
- 来源或考察方向：[MCP Elicitation](https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation)
- 题目正文：MCP Server 在执行工具过程中需要补充地址或确认选项时，Elicitation 与普通工具参数、模型自由文本追问有何不同？客户端如何渲染 Schema、处理 accept/decline/cancel，并防止服务端借此索取密码、API Key 等敏感信息？
