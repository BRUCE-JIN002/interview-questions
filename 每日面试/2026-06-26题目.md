# 2026-06-26 每日面试题

## 1. ES Modules 与 CommonJS 的加载差异

- 题型：八股文 / 场景题
- 难度：中等
- 可能来源或考察方向：综合整理；前端工程化、Node.js 模块系统
- 题目正文：请说明 ES Modules 和 CommonJS 在加载时机、导出绑定、循环依赖、Tree Shaking 友好性上的差异。若一个旧 Node.js 项目逐步迁移到 ESM，你会重点排查哪些兼容问题？

## 2. DOM 事件传播、委托与默认行为

- 题型：八股文 / 场景题
- 难度：中等
- 可能来源或考察方向：综合整理；浏览器事件模型
- 题目正文：请解释捕获、目标、冒泡三个阶段，以及 `stopPropagation`、`stopImmediatePropagation`、`preventDefault` 的区别。列表项中有按钮、链接和行点击跳转时，如何设计事件委托避免误触发？

## 3. TypeScript 类型收窄与自定义类型守卫

- 题型：代码题 / 八股文
- 难度：中等
- 可能来源或考察方向：综合整理；TypeScript 业务建模
- 题目正文：接口返回 `User | Org | null`，请说明 TypeScript 常见类型收窄方式，并手写一个自定义类型守卫判断对象是否为 `User`。业务代码中滥用 `as` 断言有什么风险？

## 4. 手写 `Object.create` 与原型链判断

- 题型：手写实现
- 难度：中等
- 可能来源或考察方向：综合整理；JavaScript 原型链
- 题目正文：请手写一个简化版 `objectCreate(proto)`，并说明它和构造函数 `new` 的关系。如何判断对象 `a` 是否出现在对象 `b` 的原型链上？

## 5. Promise 错误冒泡与 `unhandledrejection`

- 题型：输出分析 / 场景题
- 难度：中等
- 可能来源或考察方向：综合整理；异步错误治理
- 题目正文：请说明 Promise 链中 `throw`、`return Promise.reject()`、`catch` 后继续 `then` 的行为。浏览器中的 `unhandledrejection` 能解决哪些问题？为什么不能把它当作业务错误处理的主要方式？

## 6. React Compiler 与手动 memo 优化取舍

- 题型：场景题 / 项目经验
- 难度：偏难
- 可能来源或考察方向：综合整理；React 性能优化、编译期优化
- 题目正文：在 React 项目中大量使用 `memo`、`useMemo`、`useCallback` 可能带来哪些维护成本？如果团队引入 React Compiler，你会如何评估它对现有手动优化、Lint 规则和性能回归验证的影响？

## 7. Vue 3 `Suspense`、异步组件与加载态设计

- 题型：场景题
- 难度：中等
- 可能来源或考察方向：综合整理；Vue 3 组件架构
- 题目正文：请说明 Vue 3 中异步组件和 `Suspense` 的适用场景。一个页面由多个异步区块组成时，如何设计骨架屏、错误重试和局部降级，避免全页白屏？

## 8. 算法：最长无重复字符子串

- 题型：算法题
- 难度：中等
- 可能来源或考察方向：综合整理；滑动窗口、字符串处理
- 题目正文：给定一个字符串 `s`，返回其中不含重复字符的最长子串长度。请给出时间复杂度为 `O(n)` 的实现，并说明窗口左边界何时移动。

## 9. 前端大文件上传的切片、断点续传与秒传

- 题型：场景设计
- 难度：偏难
- 可能来源或考察方向：综合整理；浏览器能力、网络稳定性
- 题目正文：请设计一个前端大文件上传方案，要求支持切片上传、暂停继续、失败重试、进度展示、断点续传和秒传。前端需要保存哪些状态？如何避免重复上传和服务端文件损坏？

## 10. Next.js 服务端组件中的数据获取边界

- 题型：场景题 / 八股文
- 难度：偏难
- 可能来源或考察方向：综合整理；Next.js App Router、RSC
- 题目正文：请说明 Server Components 和 Client Components 的数据获取边界。为什么不建议把服务端密钥、数据库查询或大对象传入 Client Component？遇到交互型筛选页面时，你会如何拆分组件？

## 11. Node.js BFF 聚合接口的超时与降级

- 题型：系统设计 / 项目经验
- 难度：偏难
- 可能来源或考察方向：综合整理；BFF 稳定性
- 题目正文：一个 Node.js BFF 接口需要并发请求 5 个下游服务，其中 2 个是非核心数据。请设计超时、重试、并发控制、部分失败返回和日志埋点策略，并说明如何避免拖垮 Node.js 进程。

## 12. NestJS WebSocket 鉴权与连接状态管理

- 题型：场景题
- 难度：偏难
- 可能来源或考察方向：综合整理；NestJS Gateway、实时通信
- 题目正文：使用 NestJS 实现 WebSocket 网关时，如何在握手阶段完成鉴权？连接建立后如何管理用户连接、房间、心跳、断线重连和权限变更？Token 过期时应该怎么处理？

## 13. 接口版本演进与向后兼容

- 题型：系统设计 / 项目经验
- 难度：中等
- 可能来源或考察方向：综合整理；全栈接口协作
- 题目正文：一个前端依赖的订单详情接口要新增字段、修改枚举含义并废弃旧字段。请设计接口版本演进方案，说明如何做灰度、兼容、监控和回滚，避免多端同时发布造成线上问题。

## 14. AI 前端中的流式 Markdown 渲染安全

- 题型：AI 前端 / 场景题
- 难度：偏难
- 可能来源或考察方向：综合整理；AI Chat UI、XSS、安全渲染
- 题目正文：AI 对话前端需要边接收边渲染 Markdown、代码块和链接。请说明流式渲染中可能出现的安全风险和体验问题，并设计一个兼顾增量展示、XSS 防护、代码高亮和链接安全的方案。

## 15. Agent 长对话记忆压缩与上下文预算

- 题型：AI Agent / 系统设计
- 难度：偏难
- 可能来源或考察方向：综合整理；Agent 上下文管理、长期记忆
- 题目正文：一个编码 Agent 需要在长任务中保留需求、已改文件、失败命令、用户偏好和待办事项。请设计上下文压缩与记忆更新策略，说明哪些信息应放短期上下文、哪些应持久化，以及如何避免压缩后丢失关键约束。
