# 2026-06-29 每日面试题

## 1. 浏览器任务调度：微任务、渲染与 `scheduler.postTask`

- 题型：八股文 / 场景题
- 难度：偏难
- 可能来源或考察方向：综合整理；事件循环、主线程调度
- 题目正文：浏览器一帧内宏任务、微任务和渲染大致如何协作？如果搜索框输入后需要执行一段可拆分的计算，如何在 `queueMicrotask`、`requestAnimationFrame`、`requestIdleCallback` 和 `scheduler.postTask` 之间选择，避免输入卡顿和任务饥饿？

## 2. TypeScript 模板字面量类型解析路由参数

- 题型：类型体操 / 手写实现
- 难度：偏难
- 可能来源或考察方向：综合整理；TypeScript 条件类型、模板字面量类型
- 题目正文：请实现类型 `RouteParams<Path>`，把 `'/users/:id/posts/:postId'` 推导为 `{ id: string; postId: string }`，没有动态参数时推导为空对象。说明递归条件类型的终止条件，以及实际项目中是否应把复杂字符串解析全部放进类型系统。

## 3. `structuredClone`、JSON 深拷贝与可转移对象

- 题型：八股文 / 场景题
- 难度：中等
- 可能来源或考察方向：综合整理；JavaScript 对象复制、Web Worker
- 题目正文：比较 `structuredClone(value)`、`JSON.parse(JSON.stringify(value))` 和手写递归深拷贝的能力边界。哪些值不能被结构化克隆？将大型 `ArrayBuffer` 发送给 Worker 时，为什么 transfer 比复制更合适，转移后原缓冲区会发生什么？

## 4. 手写支持优先级与取消的任务调度器

- 题型：手写实现
- 难度：偏难
- 可能来源或考察方向：综合整理；Promise、并发控制、数据结构
- 题目正文：实现一个异步任务调度器：最大并发数为 2，任务有高低优先级，等待中的任务可通过 `AbortSignal` 取消；单个任务失败不能阻塞后续任务。请说明如何保证并发计数正确，以及是否需要承诺同优先级严格公平。

## 5. EventSource 流式响应的重连与一致性

- 题型：网络 / 场景设计
- 难度：偏难
- 可能来源或考察方向：综合整理；SSE、流式接口、弱网恢复
- 题目正文：使用 Server-Sent Events 推送长任务进度时，浏览器断网后自动重连可能带来重复事件或事件缺失。请设计事件 ID、`Last-Event-ID`、心跳、服务端保留窗口、去重和任务完成态协议，并比较原生 `EventSource` 与基于 `fetch` 流式读取在鉴权和错误处理上的取舍。

## 6. React Activity 与“隐藏但保留状态”的界面设计

- 题型：场景题 / 项目经验
- 难度：偏难
- 可能来源或考察方向：综合整理；React 19、界面状态管理
- 题目正文：多标签编辑器切换标签时，产品要求保留未提交表单状态，同时隐藏页面不应继续执行高频副作用。请说明“卸载组件”“CSS 隐藏”和 React Activity 边界在状态保留、副作用及性能上的区别，并给出上线前的验证指标。

## 7. Vue SSR Hydration 不匹配的定位与修复

- 题型：排障题 / 项目经验
- 难度：偏难
- 可能来源或考察方向：综合整理；Vue 3 SSR、同构渲染
- 题目正文：Vue SSR 页面在生产环境出现 hydration mismatch，组件中使用了当前时间、随机数、浏览器宽度和客户端本地存储。请解释不匹配的根因，给出逐项修复方案，并说明为何简单压制警告不是可靠解决方法。

## 8. 虚拟列表处理动态高度与滚动锚定

- 题型：场景设计 / 算法
- 难度：偏难
- 可能来源或考察方向：综合整理；前端性能、数据结构
- 题目正文：聊天记录列表包含图片、代码块等动态高度内容，需要支持十万条消息、向上加载历史消息且保持当前视口不跳动。请设计高度测量、可见区查找、缓存失效、overscan 和滚动锚定方案，并分析主要时间复杂度。

## 9. 算法：课程依赖与拓扑排序

- 题型：算法题
- 难度：中等
- 可能来源或考察方向：综合整理；图、拓扑排序、工程依赖
- 题目正文：给定课程数 `n` 和依赖数组 `prerequisites`，其中 `[a, b]` 表示学习 `a` 前必须完成 `b`。请判断能否完成全部课程，并在可行时返回一种学习顺序。要求说明如何检测环及时间、空间复杂度。

## 10. Next.js `instrumentation` 与服务端可观测性

- 题型：全栈工程 / 场景题
- 难度：偏难
- 可能来源或考察方向：综合整理；Next.js、OpenTelemetry、服务端运行时
- 题目正文：Next.js App Router 应用需要追踪一次请求经过 Server Component、Route Handler 和下游 API 的耗时。你会如何使用 instrumentation 接入 tracing，传播 trace context，并控制敏感属性和采样率？Node.js Runtime 与 Edge Runtime 的实现边界有哪些？

## 11. Node.js `worker_threads` 的任务池设计

- 题型：全栈工程 / 系统设计
- 难度：偏难
- 可能来源或考察方向：综合整理；Node.js、CPU 密集任务
- 题目正文：Node.js 服务需要执行图片哈希和大批量 JSON 转换。为什么把任务包装成 Promise 仍会阻塞事件循环？请设计一个 `worker_threads` 任务池，说明队列、超时、Worker 崩溃恢复、数据传输和过载保护策略。

## 12. NestJS CQRS 与事务一致性边界

- 题型：全栈工程 / 架构题
- 难度：偏难
- 可能来源或考察方向：综合整理；NestJS CQRS、领域事件
- 题目正文：订单系统计划使用 NestJS CQRS，把创建订单写模型与订单查询读模型分离。请说明 Command、Query、Event 的职责；数据库事务提交与事件发布之间如何避免丢事件；什么规模的系统不值得引入 CQRS？

## 13. OAuth 2.1 / OIDC 的前后端登录安全

- 题型：全栈工程 / 场景设计
- 难度：偏难
- 可能来源或考察方向：综合整理；鉴权、Authorization Code + PKCE
- 题目正文：为 Web 应用设计第三方登录流程，请说明 OAuth 授权码模式结合 PKCE、`state`、`nonce` 的作用。若采用 BFF 保存 token，应如何设置 Cookie、刷新会话、防止 CSRF，并避免把 access token 暴露给浏览器脚本？

## 14. MCP Sampling 的授权与安全边界

- 题型：AI / Agent 场景设计
- 难度：偏难
- 可能来源或考察方向：综合整理；MCP、Sampling、Human in the Loop
- 题目正文：一个 MCP Server 希望通过 Sampling 请求宿主模型总结内部数据。请设计宿主端的授权流程：如何展示请求来源、模型输入、可调用工具和数据范围；哪些请求需要用户确认；如何防止 Server 借 Sampling 绕过宿主的权限和提示注入防护？

## 15. Agent 端到端评测与回归门禁

- 题型：AI / Agent 系统设计
- 难度：偏难
- 可能来源或考察方向：综合整理；Agent Evals、可观测性、质量工程
- 题目正文：一个能搜索知识库并调用业务工具的客服 Agent 经常“答案看似正确但调用了错误工具参数”。请设计离线数据集、轨迹级指标、结果级指标、评审器校准、线上抽样和发布门禁，并说明如何区分模型问题、检索问题、工具定义问题和业务接口问题。
