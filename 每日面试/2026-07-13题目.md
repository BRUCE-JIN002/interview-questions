# 2026-07-13 全栈偏前端每日面试题

## 1. Proxy 与 Reflect：响应式系统的代理不变量

- 题型：原理 + 场景题
- 难度：中高级
- 考察方向：JavaScript、Vue 响应式
- 来源：综合整理
- 题目：为什么 Proxy 的 `get`、`set` 等 trap 通常要配合 `Reflect`？请解释代理不变量（invariant）是什么；若对象存在不可写、不可配置属性，错误的 `set` trap 会带来什么结果？结合响应式系统说明实现要点。

## 2. TypeScript `satisfies`、`as const` 与类型断言

- 题型：类型设计
- 难度：中高级
- 考察方向：TypeScript
- 来源：综合整理
- 题目：为路由配置设计既能校验对象形状、又保留字面量路径和权限键的写法。分别说明 `as`、`as const`、`satisfies` 的作用与误用风险。

## 3. ES2024 `Object.groupBy` 与数据分组边界

- 题型：语言特性 + 手写题
- 难度：中级
- 考察方向：ES6+、数据处理
- 来源：综合整理
- 题目：`Object.groupBy` 与 `Map.groupBy` 分别适用于什么键类型？请手写一个兼容 `Object.groupBy` 基本语义的 `groupBy`，并说明原型污染、稀疏数组和键转换的处理。

## 4. 浏览器渲染：避免强制同步布局

- 题型：性能场景题
- 难度：中高级
- 考察方向：浏览器、性能优化
- 来源：综合整理
- 题目：某拖拽面板在 `pointermove` 中循环写 `style.left` 后立即读取每个元素的 `offsetHeight`，导致 INP 升高。解释发生了什么，并给出不改变交互语义的治理方案和验证指标。

## 5. HTTP 缓存：`stale-while-revalidate` 的页面体验设计

- 题型：网络 + 场景设计
- 难度：中高级
- 考察方向：HTTP、缓存一致性
- 来源：综合整理
- 题目：内容列表允许短暂旧数据，但结算页绝不能展示旧价格。如何使用 `Cache-Control` 中的 `max-age`、`stale-while-revalidate`、`stale-if-error` 设计两类资源？前端何时需要显式刷新或展示数据时间？

## 6. React 列表 key、状态保留与重置

- 题型：框架原理 + 排障
- 难度：中高级
- 考察方向：React、渲染一致性
- 来源：综合整理
- 题目：订单列表按筛选条件切换后，某行编辑框的内容“跑到另一条订单”。解释 React 如何根据位置、组件类型和 key 保留状态；给出稳定 key、显式重置状态和避免 index key 的方案。

## 7. Vue 3 `effectScope` 在可复用逻辑中的资源回收

- 题型：框架原理 + 工程题
- 难度：中高级
- 考察方向：Vue 3、内存泄漏
- 来源：综合整理
- 题目：一个脱离组件生命周期运行的筛选器模块创建了多个 `watch`、定时器和 WebSocket 订阅。如何用 `effectScope` 组织其创建与销毁？哪些资源仍必须手动清理？

## 8. 手写：支持组合取消的 `AbortSignal.any` polyfill

- 题型：手写实现
- 难度：中高级
- 考察方向：Promise、事件、资源清理
- 来源：综合整理
- 题目：手写 `abortSignalAny(signals)`：任一输入 signal abort 时返回 signal 应立即 abort，并使用第一个 abort reason；需处理已 abort 的输入、空数组和监听器释放。说明如何测试内存泄漏风险。

## 9. 算法：编辑距离

- 题型：算法题
- 难度：中高级
- 考察方向：动态规划
- 来源：综合整理
- 题目：给定字符串 `word1`、`word2`，返回将前者转为后者所需的最少插入、删除、替换次数。给出状态定义、转移方程、复杂度，并将空间优化为一维数组。

## 10. Next.js：Server Action 的鉴权、校验与重放防护

- 题型：全栈场景题
- 难度：高级
- 考察方向：Next.js、Web 安全
- 来源：综合整理
- 题目：用 Server Action 提交“修改收货地址”表单时，哪些鉴权和输入校验必须在服务端重新执行？如何防止重复提交、跨用户越权和 Action 被直接调用？不要只依赖客户端禁用按钮。

## 11. Node.js：背压下的 CSV 导出接口

- 题型：全栈设计 + Node.js
- 难度：高级
- 考察方向：Stream、数据库、稳定性
- 来源：综合整理
- 题目：设计一个导出百万订单 CSV 的接口，要求不撑爆 Node 内存、客户端断开可停止工作、失败可观测。请描述 DB 读取、Transform、HTTP 响应的流式连接、背压与取消链路。

## 12. NestJS：Webhook 验签、幂等与异步处理

- 题型：全栈场景题
- 难度：高级
- 考察方向：NestJS、支付回调、安全
- 来源：综合整理
- 题目：第三方支付 Webhook 可能重试、乱序、并发到达。如何在 NestJS 中保留原始请求体进行签名校验，并设计去重、状态迁移、快速响应和异步消费？

## 13. 接口设计：游标分页的排序与一致性契约

- 题型：接口设计
- 难度：中高级
- 考察方向：数据库、API
- 来源：综合整理
- 题目：为按 `created_at DESC` 展示的消息列表设计 cursor pagination。cursor 里应包含什么、为何需要稳定次序、删除和新插入会造成哪些边界现象？前端刷新与“加载更多”如何协作？

## 14. AI 前端：流式工具调用的可恢复交互

- 题型：AI 前端场景题
- 难度：高级
- 考察方向：流式响应、Tool Calling、产品可靠性
- 来源：综合整理
- 题目：聊天 Agent 在流中先输出文本、再发起查库存工具调用，网络中断后用户刷新页面。请设计消息事件模型、持久化粒度、UI 状态（pending/running/needs-confirmation/completed/failed）和恢复策略，避免重复执行有副作用的工具。

## 15. Agent：RAG 与工具结果的可信边界

- 题型：AI/Agent 系统设计
- 难度：高级
- 考察方向：RAG、MCP、提示注入、安全与可观测性
- 来源：综合整理
- 题目：企业知识库 Agent 同时检索文档并读取 MCP 工具结果。如何区分“数据内容”和“指令”，防止检索片段或工具返回诱导模型泄露数据、越权调用？请给出输入处理、权限、输出校验和审计的分层方案。
